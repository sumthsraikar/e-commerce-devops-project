apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: prod
spec:

  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
    spec:
      containers:
        - name: search-service
          image: sumithsraikar/e-commerce-devops-project:search-service-f36529f9703b29ce59c8b7236f7925e23ad03916
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5002
          resources:
            requests:
              cpu: "100m"
              memory: "100Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 5002
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5002
            initialDelaySeconds: 15
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: prod
spec:
  type: ClusterIP
  selector:
    app: search-service
  ports:
    - port: 5002
      targetPort: 5002
      protocol: TCP


